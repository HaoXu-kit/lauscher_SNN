# === JAX (CUDA 12 wheels) ===
# 说明：用 pip 的专用索引安装 jax GPU 轮子，见下方安装命令
jax[cpu]

# === 科学计算 & 可视化 ===
numpy>=1.26
scipy>=1.12
matplotlib>=3.8
scikit-learn>=1.4
numba

# === 深度学习（用于桥接/调试） ===
torch>=2.1          # （服务器已有 CUDA 驱动即可；不强制装 CUDA Toolkit）
torchvision
torchaudio

# === 音频前端 ===
soundfile>=0.12
librosa>=0.10.1

# === JAX-SNN 原仓库依赖 ===
wandb>=0.17
tonic>=1.3
h5py>=3.10
pydantic>=2.7
hydra-core>=1.3

# === Hugging Face datasets（注意 2.x） ===
# Speech Commands v0.02 使用 loading script，需 <3.0
datasets[audio]<3.0

# === 工具 ===
tqdm>=4.66